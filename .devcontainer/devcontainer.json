{
  "name": "Lookout Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
"containerEnv": {
  "ENV": "codespaces",
  "FLASK_APP": "app.main",
  "FLASK_ENV": "development",
  "PYTHONPATH": "/workspaces/lookout-app",
  "SQL_SERVER_USER_CODESPACES": "${{ secrets.SQL_SERVER_USER_CODESPACES }}", 
  "SQL_SERVER_PASSWORD_CODESPACES": "${{ secrets.SQL_SERVER_PASSWORD_CODESPACES }}", 
  "SQL_SERVER_CONTAINER_SERVICE_CODESPACES": "${{ secrets.SQL_SERVER_CONTAINER_SERVICE_CODESPACES }}" 
},
"features": {},
"customizations": {
  "vscode": {
    "settings": {
      "python.defaultInterpreterPath": "/usr/local/bin/python",
      "terminal.integrated.defaultProfile.linux": "bash"
    },
    "extensions": [
      "ms-python.python",
      "ms-azuretools.vscode-docker",
      "ms-vscode.azurecli"
    ]
  }
},
  "forwardPorts": [5000],
  "postCreateCommand": "apt-get update && apt-get install -y gh && pip install -r requirements.txt",
  "remoteUser": "vscode",
  "workspaceFolder": "/workspaces/lookout-app"
}